<odoo>

    <!-- Acción principal RCV -->
    <record id="action_l10n_cl_rcv_import" model="ir.actions.act_window">
        <field name="name">RCV SII</field>
        <field name="res_model">l10n_cl.rcv.import</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- Menú raíz bajo Contabilidad -->
    <menuitem
        id="menu_l10n_cl_rcv_root"
        name="RCV SII"
        parent="account.menu_finance"
        sequence="90"/>

    <!-- Menú Importaciones -->
    <menuitem
        id="menu_l10n_cl_rcv_import"
        name="Importaciones RCV"
        parent="menu_l10n_cl_rcv_root"
        action="action_l10n_cl_rcv_import"
        sequence="10"/>

</odoo>